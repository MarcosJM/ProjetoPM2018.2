package model;

/**
 * Classificacao dada a itens da classe Periodico e Conferencia, e 
 * consequentemente a um Artigo associado a estes.
 *
 */
public enum QualisEnum {
	// B2, B3, B4 ou B5 sao de uma categoria (nomeado no sistema como qualis completo).
	// A1, A2, B1 sao de outra (nomeado no sistema como qualis restrito).
	
	A1, A2, B1, B2, B3, B4, B5;
}
